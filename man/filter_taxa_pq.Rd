% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{filter_taxa_pq}
\alias{filter_taxa_pq}
\title{Filter taxa in a phyloseq object}
\usage{
filter_taxa_pq(physeq, ..., clean_phyloseq_object = TRUE)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{...}{\if{html}{\out{<data-masking>}} Expressions that return a logical value, evaluated
in the context of tax_table. Multiple conditions are combined with \code{&}.
Use \code{.} to refer to the phyloseq object (e.g., \code{taxa_sums(.) > 100}).}

\item{clean_phyloseq_object}{if TRUE (default), the resulting phyloseq object
is cleaned using \code{clean_pq()} to remove empty taxa/samples.}
}
\value{
A phyloseq object with filtered taxa.
}
\description{
Filter taxa using data masking on tax_table. Supports the \code{.} pronoun
to refer to the phyloseq object for use with functions like \code{taxa_sums()}.
}
\examples{
library(MiscMetabar)
# Filter by taxonomy
filter_taxa_pq(data_fungi, Phylum == "Basidiomycota")

# Filter by total abundance
filter_taxa_pq(data_fungi, taxa_sums(.) > 100)

# Combine multiple conditions
filter_taxa_pq(data_fungi, Phylum == "Basidiomycota", taxa_sums(.) > 100)

# Keep taxa above median abundance
filter_taxa_pq(data_fungi, taxa_sums(.) > median(taxa_sums(.)))

}
\author{
Adrien Taudière
}
