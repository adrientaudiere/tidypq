% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncross2.R
\name{plot_uncross2_scores}
\alias{plot_uncross2_scores}
\title{Plot UNCROSS2 cross-talk scores against OTU abundance}
\usage{
plot_uncross2_scores(scores_list, grey_non_significant = TRUE)
}
\arguments{
\item{scores_list}{(list, required) The list returned by \code{\link[=uncross2_pq]{uncross2_pq()}}
with \code{return_scores = TRUE}. Must contain \code{old_physeq} (the input object),
the \code{scores} matrix, and the \code{tmin} threshold.}

\item{grey_non_significant}{(logical, default \code{TRUE}) Draw occurrences
scoring at or below \code{tmin} in grey rather than on the colour scale.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Diagnostic scatter plot for the output of \code{\link[=uncross2_pq]{uncross2_pq()}} run with
\code{return_scores = TRUE}. Each point is one non-zero occurrence (an OTU in a
sample), placed by the OTU's total abundance across the object (x) and the
read count of that occurrence (y), and coloured by its UNCROSS2 score.
Occurrences scoring at or below \code{tmin} (kept as genuine signal) are drawn in
grey; those at or above \code{tmin} (flagged as cross-talk) are coloured on the
score scale. The grey 1:1 line marks where an occurrence equals the OTU
total: genuine "source" samples sit near it, cross-talk sits far below. Both
axes are on a log10 scale.
}
\examples{
library(MiscMetabar)
data(data_fungi)
\donttest{
res <- uncross2_pq(data_fungi, return_scores = TRUE)
plot_uncross2_scores(res)
}
}
\seealso{
\code{\link[=uncross2_pq]{uncross2_pq()}}, which produces the scored list.
}
\author{
Adrien Taudière
}
