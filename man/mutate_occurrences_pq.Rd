% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occurrences.R
\name{mutate_occurrences_pq}
\alias{mutate_occurrences_pq}
\title{Transform OTU table values}
\usage{
mutate_occurrences_pq(physeq, expr)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{expr}{An expression evaluated on the OTU matrix. The result replaces
the original values. Use \code{.} to refer to cell values.}
}
\value{
A phyloseq object with transformed OTU values.
}
\description{
Apply a transformation to all values in the OTU table. This is useful for
computing relative abundances, log transformations, or other value-level
operations.

The expression is evaluated vectorized across the entire OTU matrix with
access to special variables (all as matrices matching OTU dimensions):
\itemize{
\item \code{.} = cell values (the OTU matrix)
\item \code{sample_total} = sum of each sample (repeated per column)
\item \code{taxon_total} = sum of each taxon (repeated per row)
\item \code{sample_mean} = mean of each sample (repeated per column)
\item \code{taxon_mean} = mean of each taxon (repeated per row)
\item \code{sample_median} = median of each sample (repeated per column)
\item \code{taxon_median} = median of each taxon (repeated per row)
}
}
\examples{
library(MiscMetabar)
# Convert to relative abundance (proportion of sample total)
mutate_occurrences_pq(data_fungi, . / sample_total)

# Log transformation (adding pseudocount)
mutate_occurrences_pq(data_fungi, log1p(.))

# Center by taxon mean
mutate_occurrences_pq(data_fungi, . - taxon_mean)
}
\author{
Adrien Taudière
}
