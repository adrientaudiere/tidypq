% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occurrences.R
\name{filter_occurrences_pq}
\alias{filter_occurrences_pq}
\title{Filter occurrences in the OTU table}
\usage{
filter_occurrences_pq(physeq, condition, clean_phyloseq_object = TRUE)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{condition}{An expression evaluated on the OTU matrix. Values where the
condition is FALSE (or NA) are set to 0. Use \code{.} to refer to cell values.}

\item{clean_phyloseq_object}{if TRUE (default), the resulting phyloseq object
is cleaned using \code{clean_pq()} to remove empty taxa/samples.}
}
\value{
A phyloseq object with filtered OTU values.
}
\description{
Set OTU table values to 0 based on a condition. This is useful for removing
singletons, low-abundance values, or other filtering operations at the
cell level.

The condition is evaluated vectorized across the entire OTU matrix with
access to special variables (all as matrices matching OTU dimensions):
\itemize{
\item \code{.} = cell values (the OTU matrix)
\item \code{sample_total} = sum of each sample (repeated per column)
\item \code{taxon_total} = sum of each taxon (repeated per row)
\item \code{sample_mean} = mean of each sample (repeated per column)
\item \code{taxon_mean} = mean of each taxon (repeated per row)
}

Values that do not satisfy the condition are set to 0.
}
\examples{
library(MiscMetabar)
# Remove singletons (abundance = 1)
filter_occurrences_pq(data_fungi, . > 1)

# Keep only values above 0.01\% of sample total
filter_occurrences_pq(data_fungi, . / sample_total > 0.0001)

# Keep only values above taxon mean
filter_occurrences_pq(data_fungi, . > taxon_mean)
}
\author{
Adrien Taudière
}
