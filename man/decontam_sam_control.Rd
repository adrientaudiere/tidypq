% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{decontam_sam_control}
\alias{decontam_sam_control}
\title{Decontaminate based on negative/blank control samples}
\usage{
decontam_sam_control(
  physeq,
  control_condition,
  fun = max,
  global_threshold = FALSE,
  remove_controls = FALSE,
  clean_phyloseq_object = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{control_condition}{\if{html}{\out{<data-masking>}} An expression evaluated on sample_data
that returns TRUE for control samples. Use \code{.} to refer to the phyloseq
object (e.g., \code{sample_type == "negative"}, \code{is_control == TRUE}).}

\item{fun}{(function, default \code{max}) A function to summarize the control
sample values for each taxon (or globally if \code{global_threshold = TRUE}).
Common choices: \code{max} (most conservative, default), \code{mean}, \code{median}, or a
custom function. The function should take a numeric vector and return a
single value.}

\item{global_threshold}{(logical, default FALSE) If TRUE, compute a single
global threshold from all control occurrences instead of per-taxon thresholds.
This applies \code{fun} to all values in the control samples.}

\item{remove_controls}{(logical, default FALSE) Whether to remove the control
samples from the output phyloseq object after decontamination.}

\item{clean_phyloseq_object}{(logical, default TRUE) Whether to clean the
resulting phyloseq object using \code{clean_pq()} to remove empty taxa/samples.}

\item{verbose}{(logical, default TRUE) Whether to print additional information.}
}
\value{
A phyloseq object with decontaminated OTU values.
}
\description{
Remove potential contaminants by setting OTU values to 0 when they are at or
below the level observed in negative/blank control samples for that particular OTU.
If multiple control are available, for each taxon a threshold is computed from the
control samples using a summary function (default: \code{max}). Occurrences in
non-control samples that are at or below this threshold are set to 0.
}
\examples{
library(MiscMetabar)
# Add a mock control column for demonstration using the 3 samples with lowest
# total abundance as controls
pq <- mutate_samdata_pq(data_fungi, is_control = sample_sums(.) < sort(sample_sums(.))[3])

# Decontaminate using max of controls as threshold (per-taxon)
decontam_sam_control(pq, is_control)

# Use a global threshold (single value for all taxa)
decontam_sam_control(pq, is_control, global_threshold = TRUE)

# Use mean instead of max (less conservative)
decontam_sam_control(pq, is_control, fun = mean)

# Keep control samples in output
decontam_sam_control(pq, is_control, remove_controls = FALSE)

# Use a custom function (e.g., 2x the max)
decontam_sam_control(pq, is_control, fun = \(x) 2 * max(x))
}
\author{
Adrien Taudière
}
