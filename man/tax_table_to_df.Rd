% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_table_to_df.R
\name{tax_table_to_df}
\alias{tax_table_to_df}
\title{Convert a \code{tax_table} into a data.frame with numeric and list columns}
\usage{
tax_table_to_df(
  physeq,
  convert = TRUE,
  split = NULL,
  sep = "/",
  taxa_names_col = "taxon"
)
}
\arguments{
\item{physeq}{(phyloseq or taxonomyTable, required) A phyloseq object
with a \code{tax_table} slot, or a \code{tax_table} object directly.}

\item{convert}{(logical, default TRUE) If TRUE, each column is passed
through \code{\link[utils:type.convert]{utils::type.convert()}} (with \code{as.is = TRUE}) so that columns
holding only numbers become numeric/integer and \code{"TRUE"}/\code{"FALSE"}
columns become logical. Columns with any non-numeric value are kept as
character.}

\item{split}{(character, optional) Names of columns to turn into list
columns by splitting each cell on \code{sep}. Useful when a rank cell packs
several values (e.g. multiple GBIF matches).}

\item{sep}{(character, default \code{"/"}) Separator used to split the
columns named in \code{split}. Passed to \code{\link[base:strsplit]{base::strsplit()}} with
\code{fixed = TRUE}.}

\item{taxa_names_col}{(character or NULL, default \code{"taxon"}) Name of the
leading column holding the taxa names. If NULL, taxa names are kept as
row names instead of a column.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per taxon and one column per
taxonomic rank, plus (unless \code{taxa_names_col} is NULL) a leading column
of taxa names.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


The \code{tax_table} slot of a phyloseq object is stored as a \strong{character}
matrix, so numeric information (e.g. a bootstrap support, a trait value)
is silently coerced to text and list-valued cells cannot be represented
at all. This helper lifts those limitations by returning a tibble in
which columns that look numeric (or logical) are converted to their
natural type, and selected columns can be split into genuine list
columns.

It does \strong{not} modify the phyloseq object; it produces a stand-alone
tibble for downstream (tidyverse) manipulation.
}
\examples{
\donttest{
library(MiscMetabar)
# Numeric-looking ranks are converted to numeric
df <- tax_table_to_df(data_fungi_mini)
df

# Keep taxa names as row names instead of a column
tax_table_to_df(data_fungi_mini, taxa_names_col = NULL)
}

\dontrun{
# Split a packed column into a list column
tax_table_to_df(data_fungi_mini, split = "Genus", sep = "_")
}
}
\seealso{
\code{\link[phyloseq:tax_table]{phyloseq::tax_table()}}
}
\author{
Adrien Taudière
}
