% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_contam_primer.R
\name{identify_contam_primer_pq}
\alias{identify_contam_primer_pq}
\title{Detect taxa whose representative sequence contains a primer}
\usage{
identify_contam_primer_pq(physeq, primers, verbose = TRUE)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a populated
\verb{@refseq} slot containing DNA sequences.}

\item{primers}{(named character vector, required) Primer sequences to search
for. IUPAC ambiguity codes are supported (e.g. \code{M}, \code{R}, \code{Y}, \code{N}). Names
are reported in the \code{matched_primers} evidence column.}

\item{verbose}{(logical, default \code{TRUE}) Print a one-line detection summary.}
}
\value{
A \link{contam_tbl} with one row per flagged taxon, \code{method = "primer"},
and the evidence columns \code{matched_primers} (comma-separated primer names)
and \code{n_reads}. Empty when no taxon embeds a primer.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


For each taxon, checks whether the representative sequence in the \verb{@refseq}
slot matches any primer in \code{primers} (forward or reverse complement) using
IUPAC-aware pattern matching. Taxa embedding a primer are likely chimeric or
otherwise artefactual.

This is a pure detector: it flags taxa and returns a \link{contam_tbl} without
modifying \code{physeq}. Use \code{\link[=filter_contam_pq]{filter_contam_pq()}} to remove the flagged taxa.
}
\examples{
library(MiscMetabar)
# Requires a phyloseq object with a refseq slot and Biostrings installed.
primers <- c(
  fwd = "CCCTACGGGGTGCASCAG",
  rev = "GGACTACVSGGGTATCTAAT"
)
identify_contam_primer_pq(data_fungi, primers)
}
\seealso{
\code{\link[=filter_contam_pq]{filter_contam_pq()}} to remove flagged taxa,
\code{\link[MiscMetabar:cutadapt_remove_primers]{MiscMetabar::cutadapt_remove_primers()}} for cutadapt-based primer removal
of fastq files.
}
\author{
Adrien Taudière
}
