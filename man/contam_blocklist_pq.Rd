% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contam_blocklist.R
\name{contam_blocklist_pq}
\alias{contam_blocklist_pq}
\title{Flag taxa whose genus belongs to a known-contaminant blocklist}
\usage{
contam_blocklist_pq(
  physeq,
  genus_blocklist = known_contaminant_genera(),
  tax_rank = "Genus",
  extra_genera = NULL,
  ignore_case = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a \code{tax_table}.}

\item{genus_blocklist}{(character, default:
\code{\link[=known_contaminant_genera]{known_contaminant_genera()}}) Vector of genus names to flag.}

\item{tax_rank}{(character, default: "Genus") Name of the \code{tax_table}
column holding genus-level assignments.}

\item{extra_genera}{(character, default: NULL) Additional genus names to
append to \code{genus_blocklist}.}

\item{ignore_case}{(logical, default: TRUE) If TRUE, matching is
case-insensitive.}

\item{verbose}{(logical, default: TRUE) If TRUE, print a \code{cli} summary
of the flagged taxa.}
}
\value{
A named list with four elements:
\describe{
\item{\code{stats}}{A \code{data.frame} with one row per taxon: \code{taxon},
\code{genus}, \code{is_known_contaminant}, \code{total_reads}, \code{prevalence}.}
\item{\code{contaminants}}{The subset of \code{stats} flagged as contaminants.}
\item{\code{blocklist}}{The genus vector actually used.}
\item{\code{summary}}{A named numeric vector summarizing the result.}
}
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Flag taxa whose genus matches a blocklist of known reagent and
laboratory contaminants. Unlike \code{\link[=contam_corr_pq]{contam_corr_pq()}} (which infers
contaminants from the reads-vs-depth relationship) and
\code{\link[=neg_control_classify_pq]{neg_control_classify_pq()}} (which uses negative-control occurrence
patterns), this method relies solely on taxonomy and therefore needs no
negative controls. It is best used as a complementary, curation-oriented
filter -- review flagged taxa before removal, as several blocklist genera
also occur as genuine environmental taxa.
}
\examples{
library(MiscMetabar)
data(data_fungi)
\donttest{
# data_fungi is fungal, so the default bacterial blocklist matches
# nothing; extend it with a genus present in the data for illustration.
res <- contam_blocklist_pq(data_fungi, extra_genera = "Mortierella")
res$summary
head(res$contaminants)
}
}
\seealso{
\code{\link[=known_contaminant_genera]{known_contaminant_genera()}}, \code{\link[=contam_corr_pq]{contam_corr_pq()}},
\code{\link[=neg_control_classify_pq]{neg_control_classify_pq()}}
}
\author{
Adrien Taudière
}
