% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primer_contamination.R
\name{identify_taxa_primer_pq}
\alias{identify_taxa_primer_pq}
\title{Detect taxa whose representative sequence contains a primer}
\usage{
identify_taxa_primer_pq(physeq, primers)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a populated
\verb{@refseq} slot containing DNA sequences.}

\item{primers}{(named character vector, required) Primer sequences to search
for. IUPAC ambiguity codes are supported (e.g. \code{M}, \code{R}, \code{Y}, \code{N}).
Names are used in the \code{matched_primers} output column.}
}
\value{
A \code{data.frame} with one row per contaminated taxon and three columns:
\describe{
\item{\code{taxon}}{Taxon name (from \code{taxa_names(physeq)}).}
\item{\code{matched_primers}}{Comma-separated names of all primers that
matched the sequence (forward or reverse complement).}
\item{\code{n_reads}}{Total reads across all samples (\code{taxa_sums(physeq)}).}
}
Returns \code{NULL} invisibly when no contaminated taxa are found.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


For each taxon in \code{physeq}, checks whether the representative sequence in
the \verb{@refseq} slot matches any primer from \code{primers} (forward or reverse
complement) using IUPAC-aware pattern matching (\code{fixed = FALSE}). Taxa whose
sequences embed a primer are likely chimeric or otherwise artefactual and
should be inspected or removed.
}
\examples{
library(MiscMetabar)
# Requires a phyloseq object with a refseq slot and Biostrings installed.
primers <- c(
  fwd = "CCCTACGGGGTGCASCAG",
  rev = "GGACTACVSGGGTATCTAAT"
)
identify_taxa_primer_pq(data_fungi, primers)
}
\seealso{
\code{\link[=filter_taxa_primer_pq]{filter_taxa_primer_pq()}} to remove the detected taxa,
\code{\link[=contam_corr_pq]{contam_corr_pq()}} for correlation-based contaminant detection,
\code{\link[MiscMetabar:cutadapt_remove_primers]{MiscMetabar::cutadapt_remove_primers()}} for cutadapt-based contaminant
removal of fastq files.
}
\author{
Adrien Taudière
}
