% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primer_contamination.R
\name{filter_taxa_primer_pq}
\alias{filter_taxa_primer_pq}
\title{Remove taxa whose representative sequence contains a primer}
\usage{
filter_taxa_primer_pq(physeq, primers, verbose = TRUE)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a populated
\verb{@refseq} slot containing DNA sequences.}

\item{primers}{(named character vector, required) Primer sequences to search
for. IUPAC ambiguity codes are supported (e.g. \code{M}, \code{R}, \code{Y}, \code{N}).
Names are used in the diagnostic message.}

\item{verbose}{(logical, default \code{TRUE}) If \code{TRUE}, prints a message
reporting the number of taxa and reads removed.}
}
\value{
A phyloseq object with primer-contaminated taxa removed.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Calls \code{\link[=identify_taxa_primer_pq]{identify_taxa_primer_pq()}} to identify taxa whose representative
sequence in \verb{@refseq} embeds any primer (forward or reverse complement),
then removes them with \code{phyloseq::prune_taxa()}. If no contaminated taxa
are found, the original object is returned unchanged.
}
\examples{
library(MiscMetabar)
# Requires a phyloseq object with a refseq slot and Biostrings installed.
primers <- c(
  fwd = "CCCTACGGGGT",
  rev = "GGTATCTAAT"
)
# Inspect first
identify_taxa_primer_pq(data_fungi, primers)
# Then remove
data_f_clean <- filter_taxa_primer_pq(data_fungi, primers)
}
\seealso{
\code{\link[=identify_taxa_primer_pq]{identify_taxa_primer_pq()}} to inspect contaminated taxa before
removal, \code{\link[=contam_corr_pq]{contam_corr_pq()}} for correlation-based contaminant detection,
\code{\link[MiscMetabar:cutadapt_remove_primers]{MiscMetabar::cutadapt_remove_primers()}} for cutadapt-based contaminant
removal of fastq files.
}
\author{
Adrien Taudière
}
