% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_contam_blocklist.R
\name{identify_contam_blocklist_pq}
\alias{identify_contam_blocklist_pq}
\title{Detect taxa whose genus belongs to a known-contaminant blocklist}
\usage{
identify_contam_blocklist_pq(
  physeq,
  genus_blocklist = known_contaminant_genera(),
  tax_rank = "Genus",
  extra_genera = NULL,
  ignore_case = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a \code{tax_table}.}

\item{genus_blocklist}{(character, default \code{\link[=known_contaminant_genera]{known_contaminant_genera()}})
Vector of genus names to flag.}

\item{tax_rank}{(character, default \code{"Genus"}) Name of the \code{tax_table}
column holding genus-level assignments.}

\item{extra_genera}{(character, default \code{NULL}) Additional genus names to
append to \code{genus_blocklist}.}

\item{ignore_case}{(logical, default \code{TRUE}) If \code{TRUE}, matching is
case-insensitive.}

\item{verbose}{(logical, default \code{TRUE}) Print a one-line detection summary.}
}
\value{
A \link{contam_tbl} with one row per flagged taxon, \code{method = "blocklist"},
and the evidence columns \code{genus}, \code{total_reads}, and \code{prevalence}. Empty
when no genus matches the blocklist.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Flags taxa whose genus matches a blocklist of known reagent and laboratory
contaminants. A taxonomy-only method requiring no negative controls, best
used as a complementary, curation-oriented filter -- review flagged taxa
before removal, as several blocklist genera also occur as genuine
environmental taxa.

This is a pure detector: it flags taxa and returns a \link{contam_tbl} without
modifying \code{physeq}. Use \code{\link[=filter_contam_pq]{filter_contam_pq()}} to remove the flagged taxa.
}
\examples{
library(MiscMetabar)
data(data_fungi)
\donttest{
# data_fungi is fungal, so the default bacterial blocklist matches nothing;
# extend it with a genus present in the data for illustration.
identify_contam_blocklist_pq(data_fungi, extra_genera = "Mortierella")
}
}
\seealso{
\code{\link[=known_contaminant_genera]{known_contaminant_genera()}} for the default blocklist,
\code{\link[=filter_contam_pq]{filter_contam_pq()}} to remove flagged taxa.
}
\author{
Adrien Taudière
}
