% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chimera.R
\name{chimera_removal_dada2}
\alias{chimera_removal_dada2}
\title{Remove chimeric sequences using dada2}
\usage{
chimera_removal_dada2(physeq, method = "consensus", return_a_list = FALSE, ...)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a refseq slot
containing DNA sequences.}

\item{method}{(character, default: "consensus") Method for chimera detection.
Passed to \code{dada2::removeBimeraDenovo()}. Options: "consensus", "pooled",
"per-sample".}

\item{return_a_list}{(logical, default: FALSE) If TRUE, returns a list with
the filtered phyloseq, kept taxa names, and chimeric taxa names.}

\item{...}{Additional arguments passed to \code{dada2::removeBimeraDenovo()}.}
}
\value{
If \code{return_a_list = FALSE} (default), returns a phyloseq object with
chimeric sequences removed. If \code{return_a_list = TRUE}, returns a list with:
\describe{
\item{physeq}{The filtered phyloseq object}
\item{kept_taxa}{Character vector of retained taxa names}
\item{chimeric_taxa}{Character vector of removed chimeric taxa names}
}
}
\description{
Applies \code{dada2::removeBimeraDenovo()} to identify and remove chimeric
sequences from a phyloseq object based on sequence abundance patterns.
}
\details{
This function extracts sequences and their abundances from the phyloseq
object, applies dada2's de novo chimera detection algorithm, and returns
a pruned phyloseq object containing only non-chimeric sequences.

The dada2 method uses sequence abundance information to identify chimeras,
assuming that chimeric sequences are less abundant than their parent
sequences.
}
\examples{
\dontrun{
library(MiscMetabar)
data(data_fungi)

# Basic usage
data_nochim <- chimera_removal_dada2(data_fungi)

# Get detailed output
result <- chimera_removal_dada2(data_fungi, return_a_list = TRUE)
cat("Removed", length(result$chimeric_taxa), "chimeric ASVs\n")

# Use pooled method
data_nochim <- chimera_removal_dada2(data_fungi, method = "pooled")
}
}
\seealso{
\code{\link[MiscMetabar:chimera_removal_vs]{MiscMetabar::chimera_removal_vs()}} for vsearch-based chimera
removal, \code{\link[=create_chimera_pq]{create_chimera_pq()}} for creating test data with synthetic
chimeras.
}
