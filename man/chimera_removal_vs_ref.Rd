% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chimera.R
\name{chimera_removal_vs_ref}
\alias{chimera_removal_vs_ref}
\title{Remove chimeric sequences using vsearch and a reference database}
\usage{
chimera_removal_vs_ref(
  physeq,
  database,
  vsearchpath = "vsearch",
  vsearch_args = "--fasta_width 0",
  return_a_list = FALSE,
  keep_temporary_files = FALSE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a refseq slot
containing DNA sequences.}

\item{database}{(character, required) Path to the reference FASTA database
used by vsearch for chimera detection (e.g. a curated ITS or 16S database).}

\item{vsearchpath}{(character, default: \code{"vsearch"}) Path to the vsearch
executable. Defaults to \code{"vsearch"} assuming it is on the system PATH.}

\item{vsearch_args}{(character, default: \code{"--fasta_width 0"}) Additional
arguments passed verbatim to the vsearch command line.}

\item{return_a_list}{(logical, default: FALSE) If TRUE, returns a list with
the filtered phyloseq, kept taxa names, and chimeric taxa names.}

\item{keep_temporary_files}{(logical, default: FALSE) If TRUE, temporary
FASTA files written to \code{tempdir()} are kept after the function returns.}
}
\value{
If \code{return_a_list = FALSE} (default), returns a phyloseq object with
chimeric sequences removed. If \code{return_a_list = TRUE}, returns a list with:
\describe{
\item{physeq}{The filtered phyloseq object}
\item{kept_taxa}{Character vector of retained taxa names}
\item{chimeric_taxa}{Character vector of removed chimeric taxa names}
}
}
\description{
Applies UCHIME reference-based chimera detection via
\href{https://github.com/torognes/vsearch}{vsearch} to identify and remove
chimeric sequences from a phyloseq object. Unlike de novo detection
(\code{\link[MiscMetabar:chimera_removal_vs]{MiscMetabar::chimera_removal_vs()}}), this method compares each query
sequence against a trusted reference database rather than inferring chimeras
from abundance patterns within the sample.
}
\details{
Sequences are written to a temporary FASTA file and passed to vsearch using
the \code{--uchime_ref} algorithm. Sequences classified as chimeras are removed;
sequences classified as non-chimeric or borderline are retained (conservative
default). Temporary files are cleaned up unless \code{keep_temporary_files = TRUE}.
}
\examples{
\dontshow{if (MiscMetabar::is_vsearch_installed()) withAutoprint(\{ # examplesIf}
library(MiscMetabar)
data(data_fungi)

\donttest{
# Use the mini UNITE database bundled with MiscMetabar.
# NOTE: this is a very small subset of the full UNITE database,
# so a high number of chimeras detected here is expected and does not
# reflect what you would observe with the complete database.
mini_db <- system.file(
  "extdata", "mini_UNITE_fungi.fasta.gz",
  package = "MiscMetabar"
)

data_nochim <- chimera_removal_vs_ref(data_fungi, database = mini_db)

# Return detailed output including chimeric taxa names
result <- chimera_removal_vs_ref(
  data_fungi,
  database = mini_db,
  return_a_list = TRUE
)
cat("Removed", length(result$chimeric_taxa), "chimeric ASVs\n")

# Benchmark against synthetic chimeras created with create_chimera_pq()
pq_chim <- create_chimera_pq(data_fungi, n_chimeras = 10)
result_bench <- chimera_removal_vs_ref(
  pq_chim$physeq,
  database = mini_db,
  return_a_list = TRUE
)
detected <- pq_chim$chimera_names[
  pq_chim$chimera_names \%in\% result_bench$chimeric_taxa
]
cat("Detected", length(detected), "/", length(pq_chim$chimera_names),
    "synthetic chimeras\n")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[MiscMetabar:chimera_removal_vs]{MiscMetabar::chimera_removal_vs()}} for de novo vsearch-based
chimera removal, \code{\link[=chimera_removal_dada2]{chimera_removal_dada2()}} for dada2-based chimera removal,
\code{\link[=create_chimera_pq]{create_chimera_pq()}} for creating test data with synthetic chimeras.
}
\author{
Adrien Taudière
}
