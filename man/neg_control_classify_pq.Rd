% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neg_control_classify.R
\name{neg_control_classify_pq}
\alias{neg_control_classify_pq}
\title{Classify taxa found in negative controls into contamination categories}
\usage{
neg_control_classify_pq(
  physeq,
  neg_control,
  min_reads_artifact = 10,
  max_samples_artifact = 2,
  max_ratio_lab_contam = 0.2,
  min_neg_samples_lab = 2,
  heatmap = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{neg_control}{\if{html}{\out{<data-masking>}} An expression evaluated on
sample_data that returns TRUE for negative-control samples (e.g.,
\code{is_control == TRUE}, \code{sample_type == "blank"}). Use \code{.} to refer to
the phyloseq object.}

\item{min_reads_artifact}{(numeric, default: 10) Maximum total reads
(across all samples) for a taxon to be considered an artifact.}

\item{max_samples_artifact}{(integer, default: 2) Maximum total number
of samples (negative + non-negative) in which a taxon may appear to be
considered an artifact.}

\item{max_ratio_lab_contam}{(numeric, default: 0.2) Maximum ratio of
non-negative-control sample occurrences to negative-control sample
occurrences (\code{n_non_neg_samples / n_neg_samples}) for a taxon to be
classified as a lab contaminant. Values near 0 indicate a taxon almost
exclusively found in controls.}

\item{min_neg_samples_lab}{(integer, default: 2) Minimum number of
negative-control samples a taxon must appear in to be eligible for the
lab-contaminant category. Prevents rare noise from being mislabeled.}

\item{heatmap}{(logical, default: TRUE) If TRUE, build a
\pkg{pheatmap} of negative-control abundances annotated by category.
Requires the \pkg{pheatmap} package.}
}
\value{
A named list with five elements:
\describe{
\item{\code{classification}}{A \code{data.frame} with one row per taxon
detected in at least one negative control. Columns include
\code{taxon}, \code{total_reads}, \code{reads_in_neg}, \code{reads_in_samples},
\code{n_neg_samples}, \code{n_non_neg_samples}, \code{ratio_non_neg_to_neg},
\code{category}, and taxonomy columns if a \code{tax_table} is present.}
\item{\code{summary_plot}}{A patchwork plot of the number of taxa and
total reads per contamination category.}
\item{\code{distribution_plot}}{A violin + jitter plot comparing the
distribution of total reads across categories (log scale).}
\item{\code{heatmap_by_category}}{A \code{wrapped_pheatmap} object (print it
with \code{print()}) or \code{NULL} when \code{heatmap = FALSE}.}
\item{\code{ordination_plot}}{A PCoA ordination (Bray-Curtis) with
negative controls highlighted, or \code{NULL} if ordination fails.}
}
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Examine taxa detected in negative-control samples and assign each to one
of three contamination categories based on read abundance and occurrence
patterns:
\itemize{
\item \strong{artifact}: very low total reads AND present in very few samples --
likely sequencing noise or index hopping.
\item \strong{lab_contaminant}: predominantly found in negative controls relative
to real samples -- likely introduced during library preparation or
extraction.
\item \strong{sample_contaminant}: detected in negative controls but also
widespread in real samples -- may represent genuine biological signal
or index bleed.
}

This complements \code{\link[=contam_corr_pq]{contam_corr_pq()}} (a correlation-based method) and
\code{\link[=neg_control_diag_pq]{neg_control_diag_pq()}} (a diagnostic figure) by producing an explicit
per-taxon category.
}
\examples{
library(MiscMetabar)
data(data_fungi)
\donttest{
# Mark the three lowest-depth samples as mock negative controls
pq <- mutate_samdata_pq(
  data_fungi,
  is_control = sample_sums(.) < sort(sample_sums(.))[4]
)
res <- neg_control_classify_pq(pq, is_control, heatmap = FALSE)
table(res$classification$category)
res$summary_plot
}
}
\seealso{
\code{\link[=contam_corr_pq]{contam_corr_pq()}}, \code{\link[=neg_control_diag_pq]{neg_control_diag_pq()}},
\code{\link[=neg_control_clean_pq]{neg_control_clean_pq()}}
}
\author{
Adrien Taudière
}
