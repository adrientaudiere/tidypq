% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_contam.R
\name{filter_contam_pq}
\alias{filter_contam_pq}
\title{Remove contaminant taxa flagged by a detector}
\usage{
filter_contam_pq(physeq, contam, clean_phyloseq_object = TRUE, verbose = TRUE)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{contam}{(contam_tbl, required) Output of an \verb{identify_contam_*_pq()}
detector. Removal is keyed on the \code{taxon} column, so taxa are matched by
name rather than position.}

\item{clean_phyloseq_object}{(logical, default \code{TRUE}) Whether to clean the
resulting object with \code{MiscMetabar::clean_pq()} to drop emptied
taxa/samples.}

\item{verbose}{(logical, default \code{TRUE}) Whether to report how many taxa
were removed.}
}
\value{
A phyloseq object with the flagged taxa removed.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


The single removal verb for contamination handling. It consumes any
\code{contam_tbl} produced by an \verb{identify_contam_*_pq()} detector (or several,
combined with \code{\link[=rbind]{rbind()}}) and prunes the flagged taxa from \code{physeq} by name.
When the \code{contam_tbl} is empty, \code{physeq} is returned unchanged.
}
\examples{
library(MiscMetabar)
\donttest{
flagged <- identify_contam_corr_pq(data_fungi, verbose = FALSE)
data_clean <- filter_contam_pq(data_fungi, flagged)
}
}
\seealso{
\code{\link[=identify_contam_corr_pq]{identify_contam_corr_pq()}} and the other \verb{identify_contam_*_pq()}
detectors that produce a \code{contam_tbl}.
}
\author{
Adrien Taudière
}
