% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify_contam_chimera.R
\name{identify_contam_chimera_pq}
\alias{identify_contam_chimera_pq}
\title{Detect chimeric taxa with dada2 or vsearch}
\usage{
identify_contam_chimera_pq(
  physeq,
  method = c("dada2", "vsearch_ref"),
  dada2_method = "consensus",
  database = NULL,
  vsearchpath = "vsearch",
  vsearch_args = "--fasta_width 0",
  keep_temporary_files = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object with a populated
\verb{@refseq} slot containing DNA sequences.}

\item{method}{(character, default \code{"dada2"}) Detection engine: \code{"dada2"} for
de novo abundance-based detection, or \code{"vsearch_ref"} for reference-based
UCHIME detection (requires \code{database}).}

\item{dada2_method}{(character, default \code{"consensus"}) Passed as \code{method} to
\code{dada2::removeBimeraDenovo()} when \code{method = "dada2"}. One of
\code{"consensus"}, \code{"pooled"}, \code{"per-sample"}.}

\item{database}{(character, default \code{NULL}) Path to the reference FASTA
database used by vsearch. Required when \code{method = "vsearch_ref"}.}

\item{vsearchpath}{(character, default \code{"vsearch"}) Path to the vsearch
executable.}

\item{vsearch_args}{(character, default \code{"--fasta_width 0"}) Extra arguments
passed verbatim to vsearch.}

\item{keep_temporary_files}{(logical, default \code{FALSE}) Keep the temporary
FASTA files written to \code{tempdir()} (vsearch only).}

\item{verbose}{(logical, default \code{TRUE}) Print a one-line detection summary.}

\item{...}{Additional arguments passed to \code{dada2::removeBimeraDenovo()}.}
}
\value{
A \link{contam_tbl} with one row per chimeric taxon, \code{method = "chimera"},
and the evidence columns \code{chimera_method} (\code{"dada2"} or \code{"vsearch_ref"})
and \code{n_reads}. Empty when no chimera is detected.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Flags chimeric taxa using either \code{dada2::removeBimeraDenovo()} de novo
detection (\code{method = "dada2"}) or UCHIME reference-based detection via
\href{https://github.com/torognes/vsearch}{vsearch} (\code{method = "vsearch_ref"}).

This is a pure detector: it flags taxa and returns a \link{contam_tbl} without
modifying \code{physeq}. Use \code{\link[=filter_contam_pq]{filter_contam_pq()}} to remove the flagged taxa.
}
\examples{
\dontshow{if (rlang::is_installed("dada2")) withAutoprint(\{ # examplesIf}
library(MiscMetabar)
data(data_fungi)
\donttest{
flagged <- identify_contam_chimera_pq(data_fungi, method = "dada2")
flagged
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=filter_contam_pq]{filter_contam_pq()}} to remove flagged taxa, \code{\link[=create_chimera_pq]{create_chimera_pq()}}
to generate synthetic chimeras for benchmarking.
}
\author{
Adrien Taudière
}
