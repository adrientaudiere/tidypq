% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_sample_depth_pq}
\alias{plot_sample_depth_pq}
\title{Plot sample depth differences to detect outliers}
\usage{
plot_sample_depth_pq(
  physeq,
  lower_quantile = 0.1,
  threshold_quantile = 0.05,
  show_threshold = TRUE
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{lower_quantile}{(numeric, default: 0.1) Lower quantile threshold to
exclude smallest differences when computing statistics.}

\item{threshold_quantile}{(numeric, default: 0.05) Quantile used to define
the threshold for detecting large jumps.}

\item{show_threshold}{(logical, default: TRUE) Whether to color points based
on the computed threshold and show the threshold rank in the title.}
}
\value{
A ggplot object showing:
\itemize{
\item Points representing log10(difference) vs rank
\item Solid horizontal line at the mean log10(difference)
\item Dashed horizontal lines at the 5th and 95th percentiles
\item If \code{show_threshold = TRUE}, points colored by whether they pass the
threshold
}
}
\description{
Creates a diagnostic plot showing the log10 differences between consecutive
sorted sample sums. This helps identify samples with unusually low sequencing
depth by detecting large "jumps" in the distribution.
}
\details{
The function sorts samples by their total read count (sample sums), then
computes the difference between each consecutive pair. Large differences
indicate potential outlier samples with unusually low depth.

The threshold is computed as the \code{threshold_quantile} of differences,
excluding the smallest \code{lower_quantile} of differences to avoid noise.
Samples with rank >= the first sample exceeding this threshold are
considered to have sufficient depth.
}
\examples{
library(MiscMetabar)
data(data_fungi)

# Basic plot
plot_sample_depth_pq(data_fungi)

# Adjust thresholds
plot_sample_depth_pq(data_fungi, lower_quantile = 0.2, threshold_quantile = 0.1)

# Without threshold coloring
plot_sample_depth_pq(data_fungi, show_threshold = FALSE)
}
