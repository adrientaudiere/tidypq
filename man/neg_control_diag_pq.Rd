% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negative_controls.R
\name{neg_control_diag_pq}
\alias{neg_control_diag_pq}
\title{Diagnose contamination from negative controls}
\usage{
neg_control_diag_pq(
  physeq,
  neg_control,
  top_n_heatmap = 30,
  top_n_contaminant = 20,
  log10_transform = TRUE,
  ordination_method = "PCoA",
  ordination_dist = "bray",
  palette = c(`FALSE` = "#4C72B0", `TRUE` = "#D55E00"),
  title = NULL
)
}
\arguments{
\item{physeq}{(phyloseq, required) A phyloseq object.}

\item{neg_control}{\if{html}{\out{<data-masking>}} An expression evaluated on
sample_data that returns TRUE for negative-control samples (e.g.,
\code{is_control == TRUE}, \code{sample_type == "blank"}). Use \code{.} to refer
to the phyloseq object.}

\item{top_n_heatmap}{(integer, default: 30) Maximum number of taxa
shown in the heatmap (panel C). Taxa are ranked by total reads in
negative controls.}

\item{top_n_contaminant}{(integer, default: 20) Maximum number of
taxa shown in the contaminant ranking (panel B).}

\item{log10_transform}{(logical, default: TRUE) If TRUE, apply
\code{log10(x + 1)} to the heatmap fill (panel C), the y-axis of panel
A, and the ordination input (panel D). If FALSE, raw counts are
used.}

\item{ordination_method}{(character, default: "PCoA") Method passed
to \code{\link[phyloseq:ordinate]{phyloseq::ordinate()}}.}

\item{ordination_dist}{(character, default: "bray") Distance metric
for the ordination.}

\item{palette}{(character) Length-2 named vector with entries
\code{`FALSE`} (real samples) and \code{`TRUE`} (negative controls).
Defaults to a blue/orange contrast.}

\item{title}{(character, default: NULL) Optional title placed above
the figure. The subtitle (real / NC sample counts) is added
automatically.}
}
\value{
A \link[patchwork:patchwork]{patchwork::patchwork} object with the four diagnostic
panels.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Build a four-panel patchwork figure to inspect potential contamination
from negative-control samples (extraction blanks, PCR blanks, etc.).

Panels:
\itemize{
\item \strong{A} boxplots of total reads and taxa richness per sample, split
by negative-control status.
\item \strong{B} dumbbell of the top contaminant taxa (mean relative abundance
in NC vs real samples), with mean read counts annotated above each
point.
\item \strong{C} heatmap of the taxa detected in any negative control across
all samples; absences are drawn in white, columns are faceted by
NC status.
\item \strong{D} PCoA ordination (Bray-Curtis by default) with samples
coloured by NC status.
}

The patchwork subtitle reports the number of real and negative-control
samples.
}
\examples{
library(MiscMetabar)
data(data_fungi)
# Mark the three lowest-depth samples as mock controls for demo
pq <- mutate_samdata_pq(
  data_fungi,
  is_control = sample_sums(.) < sort(sample_sums(.))[4]
)
neg_control_diag_pq(pq, is_control)
}
\author{
Adrien Taudière
}
